from tkinter import *
from TreeComponents import *

from mainWindow import MainWindow

if __name__ == '__main__':
    mainWindow = MainWindow("1280x720")
    mainWindow.run()

    # root_test = Tk()

    # node = Node()
    # node.get_node().grid(column=0, row=0)
    #
    # node2 = Node()
    # node2.get_node().grid(column=1, row=0)

    # node3 = Node()
    # node3.get_node().place(x=100, y=100)